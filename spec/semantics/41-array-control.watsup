rule PhpStep/array-literal: S ~> S_new[.TODO = (ARRAY_NEXT n phpType13*) :: ptask_tail*][.RESULT = KNOWN PNULL][.ORIGIN = $origin_first_item(S.ORIGIN, phpType13*)]
  -- if S.TODO = (EVAL (NExprArray (SEQUENCE phpType13*) metadata)) :: ptask_tail*
  -- if S_new = $allocate_array(S, $array_empty())
  -- if S_new.RESULT = KNOWN (PARRAY n)
rule PhpStep/array-finished: S ~> S[.TODO = ptask_tail*][.RESULT = KNOWN (PARRAY n)]
  -- if S.TODO = (ARRAY_NEXT n eps) :: ptask_tail*
rule PhpStep/array-unkeyed: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 1]), EVAL expression), ARRAY_VALUE n eps phpType13_tail* $runtime_line(S, [PCFIELD 1], expression)] ++ ptask_tail*]
  -- if S.TODO = (ARRAY_NEXT n ((NArrayItem ABSENT expression (BOOLEAN false) (BOOLEAN false) metadata) :: phpType13_tail*)) :: ptask_tail*
rule PhpStep/array-keyed: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 0]), EVAL expression_key), ARRAY_KEY n expression phpType13_tail*] ++ ptask_tail*]
  -- if S.TODO = (ARRAY_NEXT n ((NArrayItem phpType5 expression (BOOLEAN false) (BOOLEAN false) metadata) :: phpType13_tail*)) :: ptask_tail*
  -- if phpType5 = expression_key
rule PhpStep/array-key: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 1]), EVAL expression), ARRAY_VALUE n (S.RESULT) phpType13_tail* $runtime_line(S, [PCFIELD 1], expression)] ++ ptask_tail*][.RESULT = KNOWN PNULL]
  -- if S.TODO = (ARRAY_KEY n expression phpType13_tail*) :: ptask_tail*
rule PhpStep/array-value-unkeyed: S ~> S_out[.TODO = (ARRAY_NEXT n phpType13_tail*) :: ptask_tail*][.RESULT = KNOWN PNULL][.ORIGIN = $origin_next_item(S.ORIGIN, phpType13_tail*)]
  -- if S.TODO = (ARRAY_VALUE n eps phpType13_tail* z) :: ptask_tail*
  -- if S_read = $resolve_at(S, S.RESULT, z)
  -- if S_read.COMPLETION = NORMAL
  -- if S_read.RESULT = KNOWN pvalue
  -- if S_out = $literal_insert(S_read, n, eps, pvalue, z)
rule PhpStep/array-value-keyed: S ~> S_out[.TODO = (ARRAY_NEXT n phpType13_tail*) :: ptask_tail*][.RESULT = KNOWN PNULL][.ORIGIN = $origin_next_item(S.ORIGIN, phpType13_tail*)]
  -- if S.TODO = (ARRAY_VALUE n (poperand) phpType13_tail* z) :: ptask_tail*
  -- if S_read = $resolve_at(S, S.RESULT, z)
  -- if S_read.COMPLETION = NORMAL
  -- if S_read.RESULT = KNOWN pvalue
  -- if S_key = $key_operand(S_read, poperand, z)
  -- if S_key.COMPLETION = NORMAL
  -- if S_out = $literal_insert(S_key, n, (S_key.KEY), pvalue, z)
rule PhpStep/array-value-abrupt: S ~> S_read[.TODO = eps]
  -- if S.TODO = (ARRAY_VALUE n poperand? phpType13_tail* z) :: ptask_tail*
  -- if S_read = $resolve_at(S, S.RESULT, z)
  -- if S_read.COMPLETION =/= NORMAL
rule PhpStep/array-key-abrupt: S ~> S_key[.TODO = eps]
  -- if S.TODO = (ARRAY_VALUE n (poperand) phpType13_tail* z) :: ptask_tail*
  -- if S_read = $resolve_at(S, S.RESULT, z)
  -- if S_read.COMPLETION = NORMAL
  -- if S_key = $key_operand(S_read, poperand, z)
  -- if S_key.COMPLETION =/= NORMAL

rule PhpStep/array-reference-unkeyed: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 1]), ACQUIRE expression), ARRAY_REF_VALUE n eps phpType13_tail* $runtime_line(S, [PCFIELD 1], expression)] ++ ptask_tail*]
  -- if S.TODO = (ARRAY_NEXT n ((NArrayItem ABSENT expression (BOOLEAN true) (BOOLEAN false) metadata) :: phpType13_tail*)) :: ptask_tail*
rule PhpStep/array-reference-keyed: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 0]), EVAL expression_key), ARRAY_REF_KEY n expression phpType13_tail*] ++ ptask_tail*]
  -- if S.TODO = (ARRAY_NEXT n ((NArrayItem phpType5 expression (BOOLEAN true) (BOOLEAN false) metadata) :: phpType13_tail*)) :: ptask_tail*
  -- if phpType5 = expression_key
rule PhpStep/array-reference-key: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 1]), ACQUIRE expression), ARRAY_REF_VALUE n (S.RESULT) phpType13_tail* $runtime_line(S, [PCFIELD 1], expression)] ++ ptask_tail*][.RESULT = KNOWN PNULL]
  -- if S.TODO = (ARRAY_REF_KEY n expression phpType13_tail*) :: ptask_tail*
rule PhpStep/array-reference-value-unkeyed: S ~> S_out[.TODO = (ARRAY_NEXT n phpType13_tail*) :: ptask_tail*][.HELD = S.HELD][.RESULT = KNOWN PNULL][.ORIGIN = $origin_next_item(S.ORIGIN, phpType13_tail*)]
  -- if S.TODO = (ARRAY_REF_VALUE n eps phpType13_tail* z) :: ptask_tail*
  -- if S_out = $literal_item_insert($hold_literal_reference(S), n, eps, ALIAS S.CELL, z)
rule PhpStep/array-reference-value-keyed: S ~> S_out[.TODO = (ARRAY_NEXT n phpType13_tail*) :: ptask_tail*][.HELD = S.HELD][.RESULT = KNOWN PNULL][.ORIGIN = $origin_next_item(S.ORIGIN, phpType13_tail*)]
  -- if S.TODO = (ARRAY_REF_VALUE n (poperand) phpType13_tail* z) :: ptask_tail*
  -- if S_key = $key_operand($hold_literal_reference(S), poperand, z)
  -- if S_key.COMPLETION = NORMAL
  -- if S_out = $literal_item_insert(S_key, n, (S_key.KEY), ALIAS S.CELL, z)
rule PhpStep/array-reference-key-abrupt: S ~> S_key[.TODO = eps][.HELD = S.HELD][.RESULT = KNOWN PNULL]
  -- if S.TODO = (ARRAY_REF_VALUE n (poperand) phpType13_tail* z) :: ptask_tail*
  -- if S_key = $key_operand($hold_literal_reference(S), poperand, z)
  -- if S_key.COMPLETION =/= NORMAL

rule PhpStep/dimension: S ~> S[.TODO = [DIM_PREP (NExprArrayDimFetch expression phpType5 metadata), DIM_FETCH $runtime_line(S, [PCFIELD 1], expression_key)] ++ ptask_tail*]
  -- if S.TODO = (EVAL (NExprArrayDimFetch expression phpType5 metadata)) :: ptask_tail*
  -- if phpType5 = expression_key
rule PhpStep/global-this-dimension: S ~> $global_this_error(S, $runtime_line(S, eps, NExprVariable phpType32 metadata))[.TODO = eps]
  -- if S.TODO = (DIM_PREP (NExprVariable phpType32 metadata)) :: ptask_tail*
  -- if $direct_this(phpType32)
rule PhpStep/dimension-prepare-cv: S ~> S[.TODO = ptask_tail*][.BASE = BASE_VALUE (VARIABLE n_name* $variable_line(phpType32, metadata))][.RESULT = KNOWN PNULL]
  -- if S.TODO = (DIM_PREP (NExprVariable phpType32 metadata)) :: ptask_tail*
  -- if $cv_name(phpType32) = (n_name*)
  -- if ~$direct_this(phpType32)
  -- if ~$ppglobals_base(NExprVariable phpType32 metadata)
rule PhpStep/dimension-prepare-name: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 0]), EVAL expression), DIM_NAME $runtime_line(S, [PCFIELD 0], expression)] ++ ptask_tail*]
  -- if S.TODO = (DIM_PREP (NExprVariable phpType32 metadata)) :: ptask_tail*
  -- if $cv_name(phpType32) = eps
  -- if phpType32 = expression
rule PhpStep/dimension-name: S ~> S[.TODO = ptask_tail*][.BASE = BASE_NAME $captured_name(S, [PCFIELD 0], S.RESULT) z][.RESULT = KNOWN PNULL]
  -- if S.TODO = (DIM_NAME z) :: ptask_tail*
rule PhpStep/dimension-prepare-path: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 0]), DIM_PREP expression), DIM_INDEX expression_key $runtime_line(S, [PCFIELD 1], expression_key)] ++ ptask_tail*]
  -- if S.TODO = (DIM_PREP (NExprArrayDimFetch expression phpType5 metadata)) :: ptask_tail*
  -- if phpType5 = expression_key
  -- if ~$ppglobals_base(expression)
rule PhpStep/dimension-index: S ~> S[.TODO = [$at_task($origin_child(S.ORIGIN, [PCFIELD 1]), EVAL expression), DIM_PATH S.BASE z] ++ ptask_tail*][.BASE = BASE_VALUE (KNOWN PNULL)]
  -- if S.TODO = (DIM_INDEX expression z) :: ptask_tail*
rule PhpStep/dimension-path: S ~> S[.TODO = ptask_tail*][.BASE = BASE_DIM pbase S.RESULT z][.RESULT = KNOWN PNULL]
  -- if S.TODO = (DIM_PATH pbase z) :: ptask_tail*
rule PhpStep/dimension-prepare-value: S ~> S[.TODO = [$at_task(S.ORIGIN, EVAL expression), DIM_CAPTURE] ++ ptask_tail*]
  -- if S.TODO = (DIM_PREP expression) :: ptask_tail*
  -- if ~$delayed_base(expression)
rule PhpStep/dimension-capture: S ~> S[.TODO = ptask_tail*][.BASE = BASE_VALUE S.RESULT][.RESULT = KNOWN PNULL]
  -- if S.TODO = DIM_CAPTURE :: ptask_tail*
rule PhpStep/dimension-fetch: S ~> S_out[.TODO = ptask_tail*][.BASE = BASE_VALUE (KNOWN PNULL)]
  -- if S.TODO = (DIM_FETCH z) :: ptask_tail*
  -- if S_out = $base_read(S, S.BASE, z)

;; Ordinary compilation rejects visited omissions before runtime array building.
rule PhpStep/array-uncompiled-omission: S ~> S[.TODO = eps][.COMPLETION = UNSUPPORTED "uncompiled ordinary array omission"]
  -- if S.TODO = (ARRAY_NEXT n (ABSENT :: phpType13_tail*)) :: ptask_tail*

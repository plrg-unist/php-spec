# Reference-return compiler98

Paired code563fee7a installs compiler98/runtime99 at942/3c331e74. Compiler936/f2f8674d extends accepted933/7391 with named positional reference-return declarations whose normalized signatures already satisfy16/17/94. The [pairing report](../../coverage/semantics/reference-return-compiler-pairing.json) binds all nine copied compiler/test files and the registry addition. This is not reference-return family or full-core closure.

Compiler state carries RETURNREF only while compiling the corresponding body. Function completion restores the enclosing RETURNREF and RETURNS; normalized signature.BYREF remains source-derived through17. Existing94 records every explicit return root (including bare returns) and function.ENDLINE; existing78 preserves distinct call-instruction and enclosing post-argument emission lines.

Module98 exports pinned syntactic return-variable and return-call predicates. Variable-designated returns compile in PPW, other expressions in PPR. This reuses ordinary dimension lowering, including a temporary call dimension base, without importing parameter-send temporary restrictions. Whole$GLOBALS is variable-designated but its runtime value is an owned copy. Assignment-by-reference is value-designated even when its evaluated operand carries a reference; runtime must retain the corresponding return Notice distinction.

The nullsafe-chain check runs before writable expression compilation at the return expression's inherited Zend AST line. First934 checked at the return keyword: the preserved multiline original reports native4 versus compiler3. Only98 prelocation changed for repair; no shared call helper changed.

The maintained58-case gate has56 exact native compilation-phase comparisons,2 explicit suppression execution/compiler boundaries, and49 targeted source/mode/signature/return-line projections. Related expectation updates retire exactly the existing named-function and call-acquisition reference-return controls. Their unchanged dependent-type/variadic controls retain Unsupported. Recorded old source/native contexts remain distinct from freshly named portable phase fixtures.

Caller demand has no new compiler or runtime schema field. The separate demand-catalogue maps19 original source call occurrences to actual pinned pre-optimizer opcode result slots:14 used and5 unused. Runtime99 must assert its source-bound helper against those facts. Suppression is represented in the source predicate catalogue but remains an execution boundary. Constant ternary/coalesce redirects preserve their original value consumer, even where compilation folds them. The separate seven return-verifier opcode observations distinguish explicit verification from unreachable implicit fallthrough verification; mixed skips explicit verification. These diagnostic profiles do not replace ordinary native observations.

Preparation archive e39cda145721a1aa10564b726722c7bd6e9696f277ca98432e3d187831637dd6 and historical13b3 originals remain immutable. Closures, exception/finally/generator behavior, object-dependent types/property execution, dynamic/named/unpacked/variadic calls and other outstanding core features retain their separate continuation obligations.

The [compiler archive](../../coverage/semantics/reference-return-compiler-originals.json) preserves b6fccf718a1d98dc12a9b06adf8c27993327e14f64605f04a0c457a4cc465b4f. Source/compiler/demand and independent early gates retain942/7951a08f; typed/acquisition regressions retain942/dd8cbc49. Both bridge to final942/3c331e74 by exactly one author state-test producer file, with941 other paths and all modes unchanged. The final producer fixes do not relabel earlier runs. Runtime evidence and independent acceptance provide the separate ownership, protocol and full-state results.

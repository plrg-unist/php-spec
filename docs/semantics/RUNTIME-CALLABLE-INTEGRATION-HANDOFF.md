# Completed callable integration plan

This plan was executed and independently accepted in the
[current integration review](../../coverage/semantics/callable-integration-review.json).
Its preparation and launch instructions below are historical. Continue with the
[dynamic-call handoff](DYNAMIC-CALL-HANDOFF.md) after root reads current PROGRESS.

Start from accepted107/108 code **adb21f56**, compiler evidence **50d0fbbc** and
runtime evidence **5318ed52**, canonical **981/3f3ca6b6**. Read current PROGRESS,
[call-unpack review](../../coverage/semantics/call-unpack-review.json),
[runtime contract](SOURCE-CALL-UNPACK.md) and [compiler contract](CALL-UNPACK-COMPILER.md).
Final semantic gates retain981/ec4; the cursor repair, protocol successor and
unused-helper removal have explicit byte/mode bridges. Do not relabel earlier862d
compiler/regression gates or historical full campaigns.

Root coordinates compiler7/runtime7/review10. After root reads this accepted
checkpoint, freeze current source membership, request profiles, all implementation
bytes/modes and executable tools in separate copied roots. The prepared plan is
`.tools/compiler7-dynamic-call-next/INTEGRATION.md`; its972 membership is a starting
point, not a current981 gate. It enumerates5706 ordinary comparisons,263 explicit
request cases and696 callable catalogue rows. Add the accepted55 unpack rows for
751 callable rows across13 suites, then verify exact current lists. Counts overlap
and must not be summed as unique programs.

Compiler7 owns current full ordinary execution/outcome controls and the separate
ordered compiler lint/projection campaign. Recompute its current membership;
historical5751 compiler success is not a current count. Runtime7 owns the complete
maintained callable source suites, retaining their cache/class assertions and
explicit dependency cases. Review10 owns explicit263 requests, combined membership
and raw/tool/profile audit, and independent review of any minimized failures.
Existing final108 dense protocols need no blanket rerun without a new concern.

Before full263, validate the prepared `launch-request-campaign.py` wrapper on a
small representative complete source/native/primitive-request profile using the
accepted981 copy. The existing transport-only3-response/2-closure smoke on972
establishes transport only. The wrapper preserves request_environment main/invoke
and case profiles, substitutes existing RecordedWorker at construction, records
native argv/cwd/FD198 payload and closes remaining workers after failures. Preserve
its exact reviewed bytes and all commands/requests/streams/statuses. Reuse retained
contexts where possible; any new native invocation must serve this concrete
wrapper validation. Historical decoded-only evidence keeps its original limits.

Ordinary execution retains its no-explicit-request profile with LC_ALL=C/TZ=UTC
and inherited-environment qualification. Explicit requests retain ordered env,
argv/invocation spelling, relative/symlink paths, clock, variable order and JIT.
Callable suites retain their existing full primitive requests. Equal source bytes
under different contexts are different observations. Preserve default step/time
limits; a timeout is not a pass. Any justified allowance or stale-expectation
repair needs its exact original failure, unchanged assertions and reviewed bridge.
No case filtering or dependency relabeling may hide a mismatch. Shards must cover
the exact ordered membership once and share one immutable identity.

Exit requires all scheduled supported outcomes, separately classified dependency
and negative controls, raw request/process closures and current source/tool/mode
audits. Publish this integration checkpoint before109 admission. Full syntax,
fresh network-isolated offline rebuild, inventory completion and later combined
callable/object/lifetime validation remain required.

## Prepared next source increment, not activated

Dynamic string user calls precede function statics and closures/arrows. Preparation
is `.tools/compiler7-dynamic-call-next/PLAN.md` and `STARTUP.md`:20 original972
Unsupported contexts and9 separately instrumented opcode profiles. Literal-string
and parser-folded-concatenation callees take the static call route with known send
modes; actual variable/call-produced names use dynamic lookup and deferred sends.
Runtime string names have no lexical namespace/import fallback. Bind actual
callee evaluation and lookup before sends, non-string errors, result/reference
ownership and original source indices before reusing105/108 binding and99 returns.
Persistent static cells and effectful initialization need their own continuation
before closure capture. Objects, Traversable, builtin bodies/defaults/callbacks,
exceptions, reporting configuration and remaining core obligations stay open.

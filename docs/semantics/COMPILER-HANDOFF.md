# Compiler continuation after request and magic activation

Read workspace AGENTS, PLAN, PROGRESS, and the php/php-spec/p4-spectec skills.
Never push. The sole oracle is `.tools/php/bin/php`, PHP8.5.10 CLI NTS64, with
`vendor/php-src`. PHP meaning belongs in pure SpecTec. Unsupported, transport,
tool and budget failures never count as agreements. Complete core remains the
goal:169 constructors,70 field domains,306 obligations; only oracle identity is
closed. Final full syntax, fresh offline whole-project build and full core/source
closure remain required.

## Current checkpoint and ownership

Request/GLOBALS and all nine top-level magic constants are accepted at eed5bf8d:
code652d10b7, runtime evidence d3dfb87a, compiler evidence ca3ac883. Canonical
request identity1d206c8e has845 inputs. Its Dune adapter c0f880fa differs from the
private directly linked adapter2cea2586; independent71 transport,72 complete
states and7 CLI observations bind that build difference. Other844 inputs match.

Author source coverage is260 exact private request sources plus3 separately
routed final request sources,44 ordinary magic sources, six state programs/2,619
assertions and79 pure protocol/71 adapter controls. Private compiler5753 lints
cover the canonical5750 catalogue in exact order, with only three additional
valid magic lints now routed to request execution. It also passes8 access
sources/24 paths,15 emission lines,2 explicit pending contexts,7 metadata and
constant-export checks. Five focused groups cover GLOBALS209+70, magic47 plus
operand/context boundaries, quiet240+12, coalescing assignment84+30 and isset188
plus20 boundaries/two pending. See request-compiler-group.json for exact scopes.
The old5336 compiler report is preserved by cd0f3d3f Git blob/hash; it was not
relabeled. The earlier full container4,997+24 at96cbf051 remains historical.

Infrastructure9fc1ce9f/a934749a, independently accepted56bec412, has current
846-input identity14d17662. It adds private reference preflight and exclusive
compiler-failure preservation; no PHP semantics or fixture predicates changed.
The aggregate numeric tool deadline is600 seconds. The actual5753 aggregate
completed under that deadline. Its original120-second timeout remains a failure;
old TemporaryDirectory cleanup deleted that first fixture, so no exact replay of
that deleted fixture is claimed. The later completed19MB fixture, all native
records and raw true result are retained. A legacy outer guard then failed on
an unused canonical adapter rebuilt concurrently; actual845 inputs stayed exact.

Full quiet/CV integration is launching under runtime5 on846 inputs,
14d1766202ddc2e5ba1b0223611b1a83084f34ee7a182550a6a9771ab503462f.
The immutable campaign manifest is
`.tools/runtime5-quiet-integration/final-inputs.json`; independent preflight is
80af8f7c. It covers5706 ordinary sources plus263 separate explicit-request
sources. Its outcome is PENDING: neither preflight nor earlier targeted results
accept the full campaign. Runtime5 owns its processes, raw artifacts and eventual
report; consult its active messages/PROGRESS for launch handles and completion.
Keep this freeze intact while doing calls work privately.

Compiler4 is handing off frontend/schema/compiler ownership; runtime5 owns
runtime integration, review7 independently gates and owns PROGRESS/inventory/
phase ledger. Root orchestrates. There are no compiler4-owned processes. The
full-quiet campaign may remain active as identified above; do not mutate its
watched files or tools. New calls work belongs in distinct private snapshots;
actual calls publication follows accepted full-quiet validation.

## Private validation setup

Before freezing a private semantics root, run:

```
python3 scripts/private-semantics-references.py --copy-missing .tools/private-root
python3 scripts/private-semantics-references.py .tools/private-root
```

This provisions/hashes13 existing semantics-harness Zend/main reference files;
it refuses mismatched existing bytes and conflicting manifests. Bind its
semantics-reference-inputs.json with implementation, producer and actual tool
inputs. Full syntax/corpus checks need the full vendored tree separately.
`source_compiler.py` manually hashes Zend/zend_compile.c; the general fingerprint
alone does not include all Zend sources. Its absent-file setup failure is retained.

Do not reuse legacy `.tools/run-private-semantics.py` blindly: it binds the old
canonical adapter2c424aa8 and now fails. Bind executables actually invoked in each
private run; copied frozen executables permit canonical builds elsewhere without
changing private meaning. Suppress only a specifically identified redundant
numeric Dune build after verifying the supplied runner, never a required build.
No private Dune root or automatic binary/path repair during watched campaigns.
The checked request adapter's isolated direct OCaml build is not the mandatory
final offline whole-project audit. Coordinate all canonical builds and the index.

## Compiler and storage interfaces to preserve

- Identity is compiled unit plus original FIELD/INDEX occurrence, never AST
  equality. Nullable array items retain indices through holes. Foreach remains
  iterable0/key1/byRef2/value3/body4/keyByRef5.
- Prepass FACTS, REDIRECTS, WARNINGS, ordinary operand facts, ordered EFFECT roots
  and permanent POOLS differ. Ordinary known results do not silently become
  prepass facts. Parser literal/concat designation cannot be reconstructed from
  a later equal value. Keep original AST lines, including folded children.
- Array prepass visits values before keys and later entries after nonconstant
  operands. Hole failures precede pass-two insertion/scalar-unpack failures;
  initial holes use compilation context, later holes the preceding original
  value/unpack line. Explicit Zend barriers/traversals are not an unknown fallback.
- Required classconstant/named `::class` lookup still lacks activation. Never
  erase a potentially known lookup result to pretend it is nonconstant; retained
  builtin Attribute branch-priority originals show the actual former defect.
- PPCEFFECT/CODEEFFECT keep ordered original effects when an ordinary operand is
  known. List assignment may return IS_CONST after effects, while prepass stops
  at the assignment. CAST/coalesce remain temporaries absent earlier AST folding;
  AND/OR/NOT/comparison can fold with effects. Do not replay conversions twice.
- List nonreference CV RHS snapshots before targets and bypasses FETCH_THIS;
  reference RHS captures its wrapper. Keys precede fetches then target writes.
  Following fetch context uses original VAR line for nonreference VAR, pretarget
  fetch line for reference/DIM, and nested completion for nested patterns. Store
  diagnostics retain the separate writable target ending descriptor.
- Foreach uses insertion occurrence IDs and per-iterator saved COW-descendant
  positions, not item indexes/shared lineage progress. Metadata adds no owners.
  Preserve caller continuations during DIM setup/reset and all surviving aliases.
- Compound targets compile PPRW before RHS, keys PPR. Direct$this update/compound
  is lint-valid; lack of object context fails at runtime acquisition. Captured
  DIM opcode lines and delayed-name ordering remain essential.
- concatExprLine/nullaryExprLine/cloneExprLine and callable context have exact
  source-backed designation and edited-boundary checks. Nullary exit uses token
  spans; parenthesized exit endLine. Qualified/relative names use ordinary calls.
  Foreach reuses statementBodyLine, including UTF-16 producer profiles.

## Quiet access, names and request-wide state

PPIS preserves quiet VAR/DIM ancestors, with nonvariable bases and keys using
PPR. Coalesce fetch and isset/empty terminal protocols differ for strings and
invalid keys. Quiet property/static/nullsafe paths remain object dependencies.

HEADERASSIGNED belongs to an op-array compilation unit: direct CV W marks it,
even in compiled dead branches; R/RW/UNSET/IS do not. Direct header reads warn
only before the flag; computed constant names warn independently. Future body
compilation needs a fresh correct flag. Constant name conversion occurs during
compilation, including array/NaN diagnostics. Runtime captured_name normalizes
only when compiled_read proves that exact child occurrence constant; KNOWN alone
is insufficient. Preserve delayed CVs, original child facts and pools.

WRITES(pcpath,nat)* and CODEWRITE are distinct from read descriptors. The second
??= writable walk reuses original child facts without recompiling effects,
retaining repeated conversions where Zend does. Descriptors must be positive,
unique and associated with PPIS paths. Reference-valued temporary wrappers remain
live, while plain CV key/name operands reread after RHS. Missing/zero/duplicate
or wrongly associated descriptors reject before RHS; integrity controls remain.

Direct/parser-concat GLOBALS differs from ordinary computed names equal to it.
Whole direct GLOBALS writes/unset/reference acquisition have their static guards;
DIM keys lower as global names without header warnings or autoglobal activation
of the key. Keep distinct global unset[] errors. CVS records actual compiler
allocation order, excluding autoglobals; unbound positions survive independently
of live bindings. Ordinary$this bypasses CV allocation; nonreference list RHS$this
uses try-CV. Foreach value precedes key; for body precedes step and condition.

AUTOGLOBALS records ordered unique actual zend_is_auto_global requests at try-CV
or known computed-name sites. It carries no request values. Direct special
GLOBALS, GLOBALS[key] and synthetic second-walk bases bypass it in their special
lowering paths. An actual try-CV path, including nonreference list RHS$GLOBALS,
still records the registered benign GLOBALS lookup. Do not apply the special
bypass to every literal occurrence. CLI activates
SERVER before compilation; eager and lazy roots remain separate. HTTPROOTS and
ACTIVATED are request-wide. Callback replacement must bind a fresh global cell,
not write through an old alias. GLOBALS snapshots use symbol order and actual
reference ownership, not a precomputed native environment dump.

The checked optional request object transports only primitive ordered ENV pairs,
ARGV, invocation FILE, signed64 SECONDS, MICROSECONDS, effective VARIABLES, JIT
and optional CWD. Byte fields use canonical base64. Shape errors are transport
rejections; invalid primitive facts are explicit Unsupported. Absent request
retains php_run. Pure75/76 derive bootstrap/global behavior; native provider only
supplies external environment and clock facts, never PHP computations.

## Magic constants and next calls activation

Shared77 pmagic_value serves prepass/ordinary compilation. Optional FOLD FILE/CWD
are seeded by ppstart_cwd; ppstart delegates with absent CWD. Compiled FILE is
separate from invocation spelling used by SERVER/argv. DIR uses pure POSIX
dirname and requires explicit CWD when that result is dot. Namespace comes from
lexical ENV. Class/function/method/trait/property magic is empty only for admitted
top-level contexts; declaration bodies remain explicit boundaries. Extend real
body contexts when activating declarations; do not let top-level empties leak.
No new frontend metadata was needed. Original41 missing-magic states and actual
first helper/tool/harness failures are durable in da348660; review8cbf9998 binds
all9 source/metadata/dirname/adapter behavior.

Follow the maintained CALLS-ACTIVATION-PLAN.md (ed37a22f,5e7fb030) and existing
LINKING-HANDOFF.md. The first coherent increment must compile an actual named
function declaration/body, activate it at the proper phase, invoke a real frame,
execute recursion and return. Reuse type/signature/name/source/ownership helpers;
a task-only frame or standalone signature helper does not meet that milestone.

Each body needs its own CODE/CVS/header/loop/magic context, original declaration
identity and strictness/namespace scope. Compile effects requesting autoglobals
are request-wide, including dead bodies. Keep request-global bindings/symbol
order separate from frame ENV/CVS/SYMBOLS; GLOBALS/superglobals route to the
request table, ordinary locals to the frame. Both routes own actual shared cells.
Argument scalar strictness is at call site, return enforcement at body context.

The top-level nested-foreach return original `copy-unwind-nested-return` prints13
and skips trailing output; it remains assigned to the first return/unwind work.
Check both iterator roots and surviving aliases. Then complete parameter/reference/
named/unpack/default/variadic/closure/callable protocols; linked declarations,
objects/properties/constants; exceptions, dynamic sources, resumability, lifetime
and core intrinsics. Builtin Attribute lookup is core, not a library exclusion.
The sole intentional namespace-relative static divergence stays in its ledger;
no new divergence is authorized. Full core is still incomplete.

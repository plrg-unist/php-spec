# Independent review continuation

Read PLAN, PROGRESS, applicable AGENTS and the php/php-spec/p4-spectec skills first.
Root orchestrates; runtime3 hands over runtime/storage, compiler4 retains compiler/frontend.
The independent reviewer owns evidence, inventory, phase ledger and concise docs.
Never push. Coordinate the index and canonical Dune builds. Complete core remains
unfinished: inventory 169/306 closes only oracle identity.

## Latest accepted source closure

Ordinary code `dba21cee`, author reports `a3290ae3`/`e397b137` and independent
review `2ab579eb` pass **4,407 exact sources +25 negatives** on
`a8f6aa0cac996578befe8fa49621af2fdc214a44e4d9612a4cb8ce5dcee05691` (799 inputs).
Raw SHA-256 is `a1df0fdfcf893b067b4a581ad7a2c9e8c82a1014cfb542732b5f13cd2693695a`.
[Independent audit](../../coverage/semantics/ordinary-campaign-audit.json) verifies
ordered original bytes/IDs/outcomes, 16 archive groups and 13 current reports.
[Review](../../coverage/semantics/ordinary-review.json) binds 62 independent source
controls, 25 state programs/2,875 assertions and 4,449 compiler lints. No new
intentional divergence. README/ORDINARY-OPERATORS.md describe the admitted scope.

The 1,506 additions preserve 805 initial, 28 concat timing and 673 expanded sources.
All 680 expansion originals remain archived; exactly seven request-environment
cases are explicitly Unsupported: `concat-name-1-{4,5}` (computed this write/unset)
and `concat-name-8-{0,1,2,4,5}` (parser-concat GLOBALS read/discard/string cast/write/
unset). The original expansion report remains overall fail, with 673 agreements.
They are unfinished implementation work, not ordinary-library exclusions.

The original four concat-CV sources now pass. Parser literal designation is
separate from equal compiler facts. Concat compiles both operands before scalar
conversion warnings; constant arrays convert at runtime before delayed CV reads.
Explicit cast notices, array COW/cycles and held operand roots are preserved.

## Immediate harness prerequisite

A selected preflight overwrote ignored historical raw output before backup.
Independent recovery reproduced exact SHA
`841d9a646dcaf859348d0c6214025ebfefdb51d53b7a2cee2bbd5653867d8ef3` from the accepted
broad rows plus the surviving original context. Both selected and broad history
are backed up; ef9ed822 commits originals and the recovery proof. No observations
were lost. The production independent62 replay also initially used its old private
report filename; its truthful production fingerprint was preserved, then the
private gate rerun without claiming old private report byte identity.

Runtime3 owns the next small harness fix, after this accepted freeze: exclusive
per-run raw filenames. Only validate.py hardcodes the raw filename; consumers
follow `report.raw_results.path`, and `.gitignore` covers `coverage/results*.jsonl`.
`NamedTemporaryFile` with the existing prefix is sufficient. Repeat one selected
prefix twice and verify distinct paths, first-run byte/hash preservation, and
unchanged historical raw files. No full source rerun is needed for this narrow
IO fix if focused checks pass; the accepted full report then remains historical.
Also refresh the stale source-report scope wording. Review before commit.

## Frozen next list/unpack/effect candidate

`.tools/runtime-list-candidate/candidate-inputs.json` binds **55 modules/66 inputs,
five canonical tools and eight reports/fixtures**. Immutable; no production
admission. The author expanded report honestly has 251/252 exact agreements and
one `http_response_header` Unsupported. Separate store-line controls pass 12/12;
seven dense source controls pass 3,633 assertions (budgets 0–100); 23 mechanism
checks cover duplicate/ancestor/sibling effects, invalid paths/results, pools and
heaps. Five quiet-write boundaries remain explicitly pending.

Independent commit `d0c65f91` records **44 source/native comparisons passing** in
`coverage/semantics/list-source-review.json`, with every manifest byte rechecked:
36 effect controls plus eight coalesce/conditional controls. Original sources are
`list-independent-effects-originals.json` and `list-independent-coalesce-originals.json`.
The private replay script is `.tools/review-list-candidate-independent.py`; it uses
canonical workers without Dune. Independent mechanism/dense review and expanded
source/archive audit still remain; inspect the actual candidate and report inputs.

Core design: a list assignment can return an IS_CONST RHS while emitting writes.
Compiler-known results therefore cannot erase executable effects. `PPCEFFECT`
marks source roots separately from `PPCEXPR` results and prepass `PFFACT`s;
`CODEEFFECT`, `EVAL_EFFECT` and `COMPILED_RESULT` schedule retained effects before
returning a pooled constant. Root lookup must use the original checked occurrence,
bypass only that root's pooled entry, reject invalid paths, deduplicate markers,
filter descendants under an executed ancestor, and retain sibling order. Review
redirect filtering, ownership and resumption. No constructed replacement ASTs.

Pinned list compilation (zend_compile.c3298/3515) propagates nested references
before RHS compilation, even under an unpack operand. Nonreference direct CV RHS
bypasses compile_expr and snapshots before targets; it inherits pattern context,
including special global this behavior. List fetch diagnostics precede target
compilation. Direct/parser-concat CV names differ from computed ternaries.
Parent NOT/comparison/AND/OR may fold known results while retaining effects;
ordinary casts, coalesce and ternaries produce temporaries and may still compile
both branches. AST prepass stops at list assignment. Never transfer ordinary
operand knowledge into an effect-erasing prepass fact.

History is committed: ba11f44b preserves 56 line/RHS/spread/effect originals;
f44c85c2 preserves first64 compiler draft outcomes, four actual mismatch states,
50 loaded hashes and two empty-list context boundaries. aa706009 independently
reproduces 32+24 originals and all four exact bad ppstates, and adds 36 controls.
The private bad snapshot `.tools/list-compiler-first/loaded` remains unchanged.
Empty list() uses its existing closing line; no new metadata is needed. Compiler4
has further static/coalesce sources and compiler gates outside production.

## Foreach representation and phase ledger

Compiler4 retained `.tools/foreach-prerequisite-originals.json`: 33 originals,
including three exact PHPT exemptions (two reference-key cases and a destructured
key case). Independently repeat before mutation. Proposed `keyByRef` bool field
appends after the existing five fields, preserving occurrence indices; list keys
fit the existing expression domain. Grammar must preserve these source forms for
compiler rejection. Iterable expression context determines relevant native lines.
Gate typed/printer/schema/consumer behavior, source priority and edited metadata.
Compiler4 has `.tools/foreach-frontend-next/frontend-first-results.json`: author
33 parser comparisons, 29 checked/print roundtrips and seven typed boundaries,
binding eight frontend/schema files. All 70 domains and the original five field
indices remain unchanged. These are private author gates; obtain a stable full
candidate before independent review or source admission.
The reviewer reserves tests/phase-discrepancies.json and tests/phase_ledger.py;
retire exemptions only after preserving exact old entries and source outcomes.

## Preserved contracts and completion cadence

Nullable prerequisite `9b82507a`/reports22b36687/review91f035e0 preserves 262 source
cases, 106 metadata profiles/1,751 checks, 70 field domains and all remapped
consumers. Two redundant EOF newlines were explicitly normalized at publication.
Nullary/clone metadata have identical-complete-AST/different-native-line witnesses.
Known classconstant lookup cannot simply clear VALUE: wrong branch selection was
replaced by explicit pending lookup, not by a claimed source pass. Cast prepass
uses shared helpers with exact NaN/range/type guards.

Compound63028b5a, incdec7d5718fb and wrapper84d70299 remain covered by current source
closure. Direct this and computed rebinding differ; DIM opcode diagnostics do not
rewind final compiler context. Ordinary writable fetch never invents reference
history. ACCESS, FACTS, POOLS, held operands and source identities stay distinct.

Private-root Dune once changed shared numeric_runner build-path bytes; restored
SHA starts0787d143. Use canonical-root builds only. Main tools and every private
candidate must remain hash-bound. Ordinary helper/source literals have no new
archive dependency. Preserve original failures before repairs; Unsupported,
crashes, timeouts, interruptions and infrastructure failures never pass.

Root adopted focused independent/source/state gates per coherent increment, with
full source campaigns at integration checkpoints: unpack/list/foreach;
calls/declarations/frames; objects/properties; exceptions/dynamic/resumable services.
Unresolved shared risk can require earlier broad testing. Keep historical reports
explicitly historical, commit small compatible prerequisites, and update concise
PROGRESS after acceptance. Required environment/lookup/quiet writes, all remaining
core families, final full syntax audit and fresh offline rebuild remain unfinished.
Namespace-relative static is the sole intentional divergence, source activation
pending. Collect design decisions for the user's one final report after full core.

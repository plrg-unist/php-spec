# Independent review continuation

Read AGENTS, PLAN, PROGRESS and the php/php-spec/p4-spectec skills first.
Root orchestrates; runtime4 owns runtime/storage, compiler4 owns compiler/frontend.
Review6 owns independent evidence, inventory, phase ledger and concise docs.
Never push. Coordinate the index and canonical-root Dune builds. Pure SpecTec
executes semantics; project-local PHP 8.5.10 CLI NTS64 is the sole oracle.
Complete core remains unfinished: 169 constructors/306 obligations close only
oracle identity. Namespace-relative static is the sole intentional divergence,
with source activation pending. Final full syntax and fresh offline rebuild remain.

## Accepted containers

List code **77f8d3c0**, compiler reports **c2893aea**, runtime reports **54bda2fa**
share **f1699a75c77c253e92ea3d965bc8f0f98de6b7d51155b73e301e9c16e9ecc9aa** (812 inputs).
`destructuring-review.json` accepts 283 selected sources/24 outcome negatives,
263 additions/20 retained cases, 7 state programs/3,633 assertions, 23 mechanisms,
328 prior-family sources, 18 context checks and all five compiler gates: source
4,867; list108/metadata32; unpack42/controls11; incdec108; compound289.
`destructuring-source.json` binds exclusive raw observations; generic selected
summary was restored to historical bytes. No full current source campaign yet.

Independent sources44 (`d0c65f91`), mechanism/dense review (`29b3e2e6`) and final
integration (`b411d153`) bind 264 frozen archive observation reproductions:
263 agreements and one unchanged header Unsupported; 12 checked programs/6,192
assertions at budgets0–100; 16 extra cross-family sources. Only BUDGET resumes.
All final published semantic modules equal frozen `.tools/runtime4-containers`.
Its predecessor `.tools/runtime-list-candidate` remains immutable. Never rebuild
or overwrite either from the old preparation scripts.

Unpack code **4ca826d2**, evidence **7ac2519f**, independent acceptance **ceedbed3**
preceded list: 155 source cases/24 negatives, state31/2,454, compiler42/10 and
independent8/1,368. `array-unpack-source.json` and exact runtime112/compiler42
original archives remain durable. The empty-spread former Unsupported control is
now admitted. Initial selected-test retirement invalidated two private state
fingerprints honestly; only repeated frozen/canonical gates supply acceptance.

## Semantics and preserved defects

`DESTRUCTURING.md` and `ARRAY-UNPACK.md` describe accepted behavior and source routes.
List RHS evaluates once; key fetch precedes target preparation, nested references
propagate before RHS compilation, ordinary CV RHS uses special snapshot/context
rules, and the expression returns its original RHS result. Null list reads differ
from scalar warnings and ordinary string-offset access. Key TypeError is runtime,
while constant-array static key rejection retains its compiler phase.

Compiler-known results cannot erase stores. PPCEFFECT/CODEEFFECT name original
roots separately from PPCEXPR results/PFFACT prepass facts. Topmost ordered roots
execute once before pooled results; EVAL_EFFECT bypasses only its own root.
Redirects retain effects outside the selected subtree. Ownership, resumption,
invalid paths/results, duplicate/ancestor markers and permanent pools are reviewed.
All four compiler consumers now skip effect markers. The newly added unpack_record
helper initially failed on real list-under-unpack source; its exact tool failure,
corrected fixture and 36 checks of the earlier three consumers are durable in
`destructuring-integration-review.json`. This is not a PHP semantic discrepancy.

Four original compiler-result failures are already durable in
`list-effectful-result-first-failures.json`, `list-first-compiler-inputs.json`
(f44c85c2), independently reproduced in `list-effectful-result-review.json`
(aa706009). `.tools/list-compiler-first/loaded` preserves exact bad modules.
The earlier SpecTec anti-unification failures are separately archived in
`destructuring-first-tool-{originals,replay}.json`; never count them as semantics.
Twelve private source paths were reused by later store-line cases. Exact old/new
bytes and the filename-only stderr normalization used to replay intact archived
bytes are preserved in `list-mechanism-review.json`; no original archive was edited.

One header context case is excluded from the 263 source additions. Five quiet
coalesce cases are in `destructuring-quiet-boundaries.json`; seven earlier request
cases are in `ordinary-request-environment-boundaries.json`. All remain required
implementation work, not ordinary-library exclusions or source agreements.
Coalesce currently handles only nonvariable left operands; full quiet access,
request environment/CV diagnostics and ??= follow containers before broader calls.

## Foreach targeted acceptance and active campaign

Code **1f0e8810**, author evidence **7cdd513c**, bounded independent review
`foreach-runtime-review.json` accepts array iteration. Full 4,997 sources/24 negatives
are running; no full current closure yet. Never modify canonical watched bytes or
build tools during that run. Docs/coverage updates are safe. Root orchestrates.

Original `.tools/runtime4-foreach/final-inputs.json`819 is immutable. Independent
output replay passed143, but dense `independent-holder-element-rebound` exposed a
stale final ORIGIN. Original input reconstruction and exact failing state are durable
in `foreach-origin-first-{inputs,failure}.json` (0e13c5da). Corrected immutable
`.tools/runtime4-foreach-corrected/final-inputs.json`820 restores S.TODO before shared
DIM reset, retaining acquired LOCATION and caller HELD. Independent143 source and
8 dense programs/3,096 assertions pass; author9/6,498 pass. Public CURSOR state also
serializes at budget35. Three earlier AssignRef wrong outcomes, dead-map budget50,
raw-tuple serializer failure and the 300-second exploratory timeout remain failures
in the original archives; none count as source agreements.

Final `.tools/runtime4-foreach-publication/final-inputs.json`820 adds only12 CASES
literals. Exact selected sources, all non-CASES harness bytes, fixtures, semantics
and tools bind completed corrected state runs to current publication, without
claiming a fresh aggregate. Its819 public files equal canonical. Runtime literals
155 plus17 prechecks give172 selected cases. Full catalogue4997 adds155 unpack,
263 list,17 prechecks and155 foreach to historical4407. `foreach-runtime-bindings.json`
and `foreach-independent-evidence.json` preserve exact bridges and raw review.

The accepted prerequisite75672577/ac2aaca6 preserves FIELD0–4 and appends keyByRef
FIELD5, with70 unchanged domains. Three historical parser exceptions remain retired;
reviewer owns phase ledger. All12 previously Unsupported normal-compilation originals
are now retained unchanged in the155-source catalogue; four genuine parser controls
remain separate. Full compiler89/metadata20 has two explicit pending contexts:
header target diagnostic and nullsafe property iterable. Producer20 plain/UTF16
source/body-line and20 metadata controls passed independent replay.

Next review: audit full raw ordered membership with
`.tools/review6-container-campaign-audit.py` after runtime publishes current summary;
compare historical4407 raw hash and exact original archives. Compiler current report
should cover5039 lints after final12 additions. Preserve old5027 run as historical
with explicit correction/catalogue proof. Then update concise current status.

Quiet/CV next remains required: seven ordinary request environment, five quiet,
one list header, one return and two foreach compiler contexts are explicit work.
`QUIET-CV-REVIEW.md` and28 native originals distinguish delayed CV rereads, temporary
reuse and header write state. Independent next header prototype review passed120
native/lint/checked originals,113 source/compiler and40 compile diagnostic prefixes,
seven quiet excluded; first partial diagnostic-map failure reproduced. Its immutable
`.tools/header-runtime-checked` still uses old foreach69 and must rebase before
admission. No next-family source admission is implied.

## Historical ordinary closure and validation discipline

Ordinary code dba21cee, reports a3290ae3/e397b137, review2ab579eb passed4,407 sources
and25 negatives on a8f6aa0cac996578befe8fa49621af2fdc214a44e4d9612a4cb8ce5dcee05691.
Raw `coverage/results-semantic-source.jsonl` SHA remains
`a1df0fdfcf893b067b4a581ad7a2c9e8c82a1014cfb542732b5f13cd2693695a`.
`ordinary-campaign-audit.json` binds every original row,16archives/13reports at that
historical checkpoint. Nullable/context9b82507a, compound63028b5a, incdec7d5718fb
and wrapper84d70299 remain earlier accepted contracts, with preserved failures.
Only actual reference acquisition marks REFCELLS; ordinary writable fetch does not.

Raw protection de04828e, independent6209e75f, uses exclusive NamedTemporaryFile
outputs. Summaries are latest-run pointers; preserve durable named snapshots before
later selections. Historic selected raw recovery exactly restored SHA841d9a64;
`ordinary-selected-raw-recovery.json` retains proof. Never count Unsupported,
stuck derivations, crashes, timeouts or infrastructure failures as passing sources.
Canonical numeric runner hash starts0787d143; private-root Dune once changed it.
Use only canonical builds and hash-bind every candidate/tool/report.

Focused exact-source/compiler/state/independent gates accompany each increment.
Run full current source closure at unpack/list/foreach integration, then at
calls/frames, objects/properties and exceptions/dynamic/resumable checkpoints, or
earlier for a real unresolved shared risk. Final full syntax, offline rebuild,
current core closure and the user's one final report remain mandatory.

# Integration after accepted arrows

Read [PLAN](../../PLAN.md), [PROGRESS](../../PROGRESS.md), the
[arrow review](../../coverage/semantics/arrows-review.json) and
[core checklist](CORE-CONTINUATION-CHECKLIST.md). Arrow117/118 code 1d438e8a
is accepted on1026/0ec57507 through an exact one-test bridge from1026/365a0464.
The bridge partitions independent dense-test budget blocks; all semantic rules,
source fixtures and other test/tool bytes are identical. Measurements retain
their original identities. Explicit closures1017/9325 and broad981/f244a78e remain
historical. Complete PHP core, final syntax and fresh offline rebuilding remain open.

Arrows use real closure identities and authenticated CODEIMPLICIT/CODEARROW
metadata at the actual FIELD5 expression root. Discovery follows pinned Zend
child order, including array value before key, nested explicit use lists and
nested arrow expressions, then removes only the current parameters. Implicit
undefined snapshots are distinct from null and construction suffixes. Current
capture mode/cursor authenticates completion without replaying history; cursor=end
still owns a partial noncallable object until publication. Invocation receives
fresh value cells, including actually undefined locals.

The expression return keeps original source, line, signature and reference demand.
Never has a discarded expression and typed fallthrough, with no fabricated checked
return node. Its narrow reference-demand repair and the shared suppression-owner
fix retain actual failing paused packets. The latter also affected accepted
explicit closures: current/saved owners now use named functions plus templates,
and MAIN cannot claim their nested suppression origins. An unrelated active result
does not invalidate a valid saved return context.

Accepted focused evidence includes32 author arrow contexts, compiler20/223,
protocol26/264, regression13, old closure protocol20/217, independent source9,
dense4/731 and32 complete old1017 response bridges with no projection. The32 original
model-only replays and fresh maintained profiles are separate. The explicit
suppression counterpart is one existing-form regression, not an arrow source.
Independent dense checks compare56 one-step and56 complete resumptions.

The author COW dense timeout remains inconclusive at900 seconds, with its second
numeric case not run and both native/source setups retained. The exact test-only
repair preserves493 unique assertions and36 each zero/one/full budget checks in9
fixtures,960 executed premises, each retaining the900-second bound. It reuses the
original contexts for numeric-only replay; all nine fixtures pass on0ec5.
Archive audits bind all failures,
snapshots, actual tools and raw worker/numeric closures. Do not repeat these native
originals or claim the timed-out aggregate passed.

The next action is broad integration before further callable/class representation
changes. The detailed prepared plan is `.tools/compiler7-post-arrow-integration/PLAN.md`;
its1017 catalogue is preparation only. A fresh runtime author can recover the
prepared lane from `.tools/compiler7-post-arrow-integration/RUNTIME-HANDOFF.md`. Re-enumerate source bytes, IDs, profiles,
producer hashes, watched files/modes and executable aliases on the installed
identity before launch. Required scheduled memberships are:

- 5706 ordinary comparisons plus21 outcome controls, run by the runtime author as one
 unchanged monolithic producer after outcome-control preflight.
- 5751 compiler phases plus8 access/15 line cases, then263 explicit requests
 sequentially in compiler7's lane.
- 946 callable source/profile rows across18 complete suites, plus exactly one
 separate explicit-closure suppression regression, giving947 scheduled
 callable/regression observations. Use disjoint whole suites after a shorter
 lane frees; the other12 arrow-regression contexts already occur in those suites.

These memberships overlap across profiles and are not unique-program totals.
Ordinary absent-request environment and explicit primitive request profiles
remain distinct. Preserve exact argv/cwd/path spellings, clocks, variable-order
and JIT settings; fresh native observations belong to each scheduled context.
Callable entry alarm60 is replaced by per-packet alarm30 and canceled afterward;
it is not a continuous whole-case deadline. Compiler aggregate600, native10 and
worker-close5 limits retain their measured meaning. Verified numeric-runner reuse
is not an offline rebuild.

Capture outer process streams/status and every raw worker/native closure. Do not
split/restart the ordinary catalogue, splice a prefix, kill a running campaign or
invent resume machinery to shorten it. If an old Unsupported control has become
supported, preserve its original current/native result, review the exact source,
and make only the required test correction with an explicit identity bridge.
Actual defects require preserved minimal witnesses, narrow reviewed repairs and
impacted checks; failures never disappear from the ledger.

Independent review13 oversees membership/identity and ordinary/compiler/request
raw audits. Authors cross-audit callable suites they did not run. Root coordinates
private roots, canonical installation, Git index and small evidence/docs commits.
No broad launch before arrow code/evidence/docs/PROGRESS acceptance, and no later
representation activation during integration. Never push.

Required Closure method/bind/call/clone/fromCallable/first-class/reflection services,
method and array callables, ordinary object/class/property behavior, cycle
collection, exceptions and dynamic-source lifetime remain open. The valid
Closure::fromCallable type witness is a service dependency, not a PHP error.
Full current syntax and a fresh network-isolated offline rebuild/audit remain
mandatory before complete-core acceptance; focused arrows and integration cannot
close that goal by themselves.

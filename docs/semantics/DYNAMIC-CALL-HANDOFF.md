# Dynamic string user calls after the integration checkpoint

Read current PROGRESS and [the integration review](../../coverage/semantics/callable-integration-review.json). The current campaigns cover5706 ordinary comparisons plus21 outcome controls,263 explicit requests,751 callable rows across13 suites, and5751 compiler lint comparisons with separate projection checks. These observations overlap. Ordinary/compiler execute981/7640; request/callable execute981/3f3 through an exact one-test bridge. No semantic module changed during integration.

Root coordinates compiler7, runtime7 and review10. Root must read the accepted checkpoint before109/110 implementation. Reuse `.tools/compiler7-dynamic-call-next/{PLAN.md,STARTUP.md,records20.json}` and `.tools/runtime7-dynamic-call-next/INTERFACE110.md`. Replay the20 retained972 source/native contexts against the newly accepted baseline without duplicate native calls; nine instrumented opcode profiles remain separate evidence. The older preparation's integration-launch wording is historical: the current campaigns and representative full request preflight have completed.

The compiler partition must distinguish initial parser ZVAL strings (including parser-folded concatenation), expressions that become CONST strings only during expression compilation, and dynamic CV/VAR/TMP callees. Only the initial string route can infer known send modes. Reuse20 parser_literal and preserve87/106 initial builtin special-lowering and result-class dependencies; a dynamic call VAR does not reclassify those initial builtin results. Runtime string lookup has no lexical namespace/import fallback. Preserve original diagnostic name bytes and source-derived emitted INIT/SEND/called-in/RETURN locations. Arrays, objects and static-method strings remain explicit dependencies until their own source contracts are implemented.

Evaluate and own the callee before arguments, select the existing registered user function, release the callee operand, then reuse positional/named/unpack dispatch105/108 and return99. Later argument effects must not change that selection. Preserve source argument indices, effective positional extent, named keys, default-hole preflight, uncoerced cache ownership and reference lifetimes. Extend return-demand and unpack-class consumers to the same expression-callee lane; ordinary call VAR results cannot depend on a static CODENAME.

Before lookup, finite guards bind the actual source, emitted line and source-dependent CV name while allowing arbitrary consistent owning values. After dynamic lookup, validate the selected existing function and current source/frame/owner relations without reconstructing its previous name. Initial literal-name calls retain their source-name relation. Capture pre-fix source/state counterexamples before guards; test arbitrary consistent positives, saved frames, suppression/errors, default caches and complete old-state compatibility.

Function statics require persistent cells and effectful initialization before closures/arrows. Builtin bodies/defaults/callbacks, Traversable, objects, exceptions, reporting configuration, dynamic sources and remaining lifetime protocols remain required. The full syntax campaign, fresh network-isolated offline rebuild/audit and complete inventory are still final gates. Current integration closes no entire family or new inventory obligation.

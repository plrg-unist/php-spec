# Isset and empty review

Code `701e39fa` admits scalar/array/string `isset` and `empty` through checked
source execution. [Independent review](../../coverage/semantics/isset-empty-review.json)
binds 232 exact source outcomes, 22 separate controls, nine author state programs/
4,230 assertions and seven independent programs/2,655 assertions. Twelve fresh
canonical CLI comparisons pass. The four original GLOBALS temporary/key Unsupported
observations are retained. They now have explicit-request witnesses in the
[request publication](../../coverage/semantics/request-environment-review.json)
at652d10b7; object access and broader core work remain pending.
Earlier full source campaigns remain historical.

Module73 compiles all operands in source order, even when runtime multi-`isset`
will stop early. Invalid expression operands receive the native compile rejection
before their children execute. Nonvariable `empty` uses ordinary expression
compilation and boolean negation, retaining constant facts and child effects.
Direct `$GLOBALS` has a known compile result, while computed names retain ordinary
lookup distinctions. Positive occurrence lines are required where used; edited
metadata and explicit call/object boundaries are separate integrity controls.

Module74 treats the terminal access differently from coalescing's quiet read.
Variable ancestors use quiet access and keys retain ordinary effects. Missing
names remain absent from the environment. Terminal string offsets use the native
isset/empty conversion rules; an invalid terminal numeric string can differ from
the same string used at an intermediate dimension. Empty string and the byte
`"0"` are false values. Float/NaN diagnostics retain their selected source lines.
Global-scope direct `$this` has its special result; nested access still performs
the required base lookup. Multi-`isset` short-circuits in order and releases saved
operands on normal and abrupt completion.

Dense independent checks include mutated reference keys, array COW, missing nested
bases, abrupt second operands and foreach origins. They compare complete resumed
states across budgets0–48,64,96,128, unchanged CODE/POOLS, valid heap/source/iterator
state and empty final TODO/ORIGIN/HELD/ITERATORS. One initial dense selection was a
static rejection; its runtime-only fixture builder failed. The original script,
source and partially generated fixtures remain archived, separate from seven
passing runtime programs. The static rejection itself passes source comparison.

The paired compiler prerequisite records ordered unique CV names in the actual
Zend compiler walk. Only direct try-CV paths allocate; computed constant-name
FETCH does not. Autoglobals are excluded. Ordinary `$this` bypasses the path,
while non-reference list RHS compilation has the native direct-try exception.
Fresh34 native/lint/checked controls and34 ordered descriptor assertions pass,
plus188 isset compiler traces and10 metadata/two pending controls. This records
compiler facts; runtime symbol-table initialization is accepted separately in the
request publication.

All frozen831 semantic modules and executable bytes equal canonical835. The
232-source catalogue matches the independent source multiset exactly; original
state run identities are retained. Author gates freshly reran the durable source
and state harnesses. The raw author audit checks every ordered source hash and
native/semantic stdout, stderr and exit status. Unsupported, timeout and tool
failures never count as source agreement. No constructor family closes here.

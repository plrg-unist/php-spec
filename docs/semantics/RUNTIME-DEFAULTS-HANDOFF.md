# Runtime successor: positional untyped defaults

Constants paired code is5151608e, compiler evidence41b2c1a2 and historical default preparation3452d7d4. Full current executable input closure is899/ca3c06e5. Author evidence06b64f88 and the [independent review3341519d](../../coverage/semantics/user-constant-review.json) close this bounded checkpoint. Start the default candidate from the accepted canonical899 root once the checkpoint closes, and keep this constant candidate immutable.

Reserve compiler90/runtime91. Compiler17 pause/resume preparation is historical877 and must be transplanted surgically, never replacing current source/runtime modules. The earlier DEFAULT-MILESTONE.md used88/89 and accepted884; those reservations and base are superseded. The [compiler handoff](CONSTANTS-COMPILER-HANDOFF.md) is the paired continuity contract.

Agreed descriptor proposal: pfunction.DEFAULTS ordered parameter-index/original-origin/STORED-or-DEFERRED; stored entries borrow existing per-unit pool values. Defaults compile in declaration function namespace/magic context, under the pinned no-constant-substitution policy. Per-unit CODE includes explicit source default roots. A normalized required parameter before/after optional diagnostics has no receive descriptor. Typed/named/variadic/unpacked/reference-return source forms remain explicit later increments.

Runtime receives omitted positional parameters in order after callee activation and provided binding, before body execution. Supplied operands skip default evaluation entirely. Preserve actual argument count/too-few traces and source line context. Omitted untyped byref parameters initially own fresh ordinary cells; do not eagerly mark a reference just because signature BYREF. Actual alias acquisition can promote it. Arrays require per-call COW and escaping reference freshness.

Observable deferred receive cache must be represented in checked runtime state. Pinned RECV_INIT caches only non-refcounted results with no recorded AST side effects. Scalars and non-refcounted strings/shared empty arrays can satisfy that condition; allocated strings/arrays cannot. Failures never install a successful cache entry. Class provenance is already available from source constant folds, actual runtime operations and constant table values. Equal bytes can differ, including empty/one-byte strings and empty arrays. Do not infer the class from value length or recursively intern constant-array strings. Metadata owns no heap roots; cached actual values require explicit source-bound owner accounting and abrupt/saved-frame consistency.

Current observer machinery in89-constant-classes records actual evaluation, source-backed pool facts and quiet left results. Its guard currently allows class pool roots only beneath NConst initializers. Default admission must explicitly extend source roots and add a typed receive context; do not weaken source checks to accept arbitrary paths. Branch invariants use already recorded condition truth/left nullness without re-evaluation, and retain arbitrary consistent runtime values. Constructor fields/table/scratch additions must preserve no-default historical fullstate bridges.

Author originals40 are .tools/runtime6-defaults/.tools/default-originals-_mfy3p4x on old884 (36Unsupported/four already-normalized agreements). Additional compiler/reviewer default and cache originals are in separately committed archives, including3452d7d4. Read reviewer source catalogues and pinned RECV_INIT before expanding. Constants author3 ownership state roots and reviewer4 states are completed; do not rerun unchanged campaigns merely for counts.

=== Parameters

// Added
${syntax: direction parameter}

// Added
Callables can have parameters. Each parameter has a direction, a type, a
name, and an optional default value.

// Added
A parameter is internally represented as:

// Added
${syntax: parameterIR}

// Added
The name of a parameter, and whether it is a default or an optional parameter,
are obtained with:

// Added
${func-source: id_of_parameterIR}
${func-latex: id_of_parameterIR}
${func-prose: id_of_parameterIR}

// Added
${func-source: is_default_parameterIR}
${func-latex: is_default_parameterIR}
${func-prose: is_default_parameterIR}

// Added
${func-source: is_optional_parameterIR}
${func-latex: is_optional_parameterIR}
${func-prose: is_optional_parameterIR}

// Added
The high-level overview of parameters with respect to the copy in/copy out
calling convention is described in <<sec-call-convention-overview>>. Below are
the formal typing and well-formedness rules for parameters that implement the
restrictions described there.

==== Type checking

// Added
${relation-title-source: Parameter_ok}
${relation-title-latex: Parameter_ok}
${relation-title-prose: Parameter_ok}

// Added
Typing a parameter produces a `parameterIR` if it is well-typed. A parameter
is well-typed when its type is well-formed and its default value (if any) has
the same type as the parameter type.

// Added
${rulegroup-source: Parameter_ok}
${rulegroup-latex: Parameter_ok}
${rulegroup-prose: Parameter_ok}

// Added
A list of parameters is type checked as follows:

// Added
${relation-title-source: ParameterList_ok}
${relation-title-latex: ParameterList_ok}
${relation-title-prose: ParameterList_ok}

// Added
${rulegroup-source: ParameterList_ok}
${rulegroup-latex: ParameterList_ok}
${rulegroup-prose: ParameterList_ok}

// Added
${relation-title-source: Parameters_ok}
${relation-title-latex: Parameters_ok}
${relation-title-prose: Parameters_ok}

// Added
${rulegroup-source: Parameters_ok}
${rulegroup-latex: Parameters_ok}
${rulegroup-prose: Parameters_ok}

==== Well-formedness

// Added
${relation-title-source: ParameterType_wf}
${relation-title-latex: ParameterType_wf}
${relation-title-prose: ParameterType_wf}

// Added
A parameter type is well-formed when its type is well-formed. Also, the type
should not be a synthesized type. Types that can only be local compile-time
known or compile-time known (e.g., `int` type) must be directionless. A default
value can be specified for `in` or directionless parameters only.

${rulegroup-source: ParameterType_wf}
${rulegroup-latex: ParameterType_wf}
${rulegroup-prose: ParameterType_wf}

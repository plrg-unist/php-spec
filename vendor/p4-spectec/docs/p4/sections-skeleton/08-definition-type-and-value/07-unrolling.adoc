=== Unrolling

// Added
``typedef``s, described in <<sec-typedef>>, introduce indirection in types.

// Added
[source,p4]
----
struct S {
    bit<8> t;
    int<8> u;
}

typedef S A;
----

// Added
The type ``A`` is an alias of the struct type ``S``. Neverthelss, the
underlying type of ``A`` is a `structTypeIR` with two fields, ``t`` and ``u``,
of types ``int<8>`` and ``bit<8>`` respectively. It is useful to be able to
work with the underlying type of ``A``, when dealing with type checking, type
equivalence, and other type-based analyses.

// Added
Unrolling is the process of recursively expanding all ``typedef``s until a type
is fully expanded to its underlying type.

// Added
${func-source: unroll_typeIR}
${func-latex: unroll_typeIR}
${func-prose: unroll_typeIR}

// Added
Aliases introduced by ``type`` declarations, described in <<sec-newtype-declaration>>,
are unrolled similarly, expanding both ``type`` aliases and ``typedef``s:

// Added
${func-source: unroll_aliasTypeIR}
${func-latex: unroll_aliasTypeIR}
${func-prose: unroll_aliasTypeIR}

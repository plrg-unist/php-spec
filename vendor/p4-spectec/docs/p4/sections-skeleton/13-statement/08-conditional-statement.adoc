[#sec-conditional-statement]
=== Conditional statements

// 723
The conditional statement uses standard syntax and semantics familiar from many
programming languages.

// 724
However, the condition expression in P4 is required to be a Boolean
(and not an integer).

// 725 - Modified
${syntax: conditionalStatement}

// 726
When several `if` statements are nested, the `else` applies to the innermost
`if` statement that does not have an `else` statement.

==== Type checking

// Added
After type checking, a conditional statement has the form:

// Added
${syntax: conditionalStatementIR}

// Added
${rulegroup-source: Stmt_ok/conditionalStatement}
${rulegroup-latex: Stmt_ok/conditionalStatement}
${rulegroup-prose: Stmt_ok/conditionalStatement}

==== Compile-time evaluation

// Added
${rulegroup-source: Stmt_inst/conditionalStatementIR}
${rulegroup-latex: Stmt_inst/conditionalStatementIR}
${rulegroup-prose: Stmt_inst/conditionalStatementIR}

==== Runtime evaluation

// Added
${rulegroup-source: Stmt_eval/conditionalStatementIR}
${rulegroup-latex: Stmt_eval/conditionalStatementIR}
${rulegroup-prose: Stmt_eval/conditionalStatementIR}

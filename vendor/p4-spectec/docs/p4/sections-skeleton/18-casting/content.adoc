[#sec-cast]
== Casting

// 479 - Modified
P4 provides a limited set of casts between types. While this design is arguably
more onerous for programmers, it has several benefits:

// 480
* It makes user intent unambiguous.
* It makes the costs associated with converting numeric values
  explicit. Implementing certain casts involves sign extensions, and thus can
  require significant computational resources on some targets.
* It reduces the number of cases that have to be considered in the P4
  specification. Some targets may not support all casts.

[#sec-cast-chain]
=== Cast chains

// Added
A cast is not always a single step. Casting a sequence to a struct, for
example, casts each element of the sequence before casting the sequence as a
whole, and casting between two enums with an underlying type goes through that
underlying type. Checking a cast therefore yields the chain of types to cast
through, in order, or reports that no such cast exists.

// Added
${syntax: castResult}

// Added
The casts of the elements of an aggregate type are chained in lockstep, one
cast of the whole aggregate per step. An element whose chain is exhausted holds
its current type, so that all elements reach their target together.

// Added
${func-source: chain_casts}
${func-latex: chain_casts}
${func-prose: chain_casts}

// Added
${func-source: chain_casts'}
${func-latex: chain_casts'}
${func-prose: chain_casts'}

// Added
Each step of the chain is then wrapped back into the shape of the aggregate
type being cast, a sequence type,

// Added
${func-source: chain_casts_sequenceTypeIR}
${func-latex: chain_casts_sequenceTypeIR}
${func-prose: chain_casts_sequenceTypeIR}

// Added
${func-source: chain_casts_sequenceTypeIR_default}
${func-latex: chain_casts_sequenceTypeIR_default}
${func-prose: chain_casts_sequenceTypeIR_default}

// Added
or a record type, whose field names are kept across the chain.

// Added
${func-source: chain_casts_recordTypeIR}
${func-latex: chain_casts_recordTypeIR}
${func-prose: chain_casts_recordTypeIR}

// Added
${func-source: chain_casts_recordTypeIR_default}
${func-latex: chain_casts_recordTypeIR_default}
${func-prose: chain_casts_recordTypeIR_default}

include::01-explicit-cast.adoc[]
include::02-implicit-cast.adoc[]
include::03-cast-insertion.adoc[]
include::04-runtime-cast.adoc[]
include::05-equivalence.adoc[]

var i : int

relation Sub:
  |- int `- int `= int
  hint(input %0 %1)
rule Sub:
  |- i_a `- i_b `= i_c
  -- if i_c = $(i_a - i_b)

dec $main() : int
def $main() = i_c*[0]
  -- if i_a* = [ 1, 2, 3 ]
  -- if (i_b = $(i_a + 42))*
  -- (Sub: |- i_a `- i_b `= i_c)*

name: CI (concrete)

on:
  push:
    branches: [ concrete ]

jobs:
  test-fast:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: false
        fetch-depth: 1

    - name: Initialize p4c submodule
      run: git submodule update --init p4c

    - name: Set up OCaml
      uses: ocaml/setup-ocaml@v3
      with:
        ocaml-compiler: 5.1.0
        dune-cache: true

    - name: Cache opam switch
      uses: actions/cache@v4
      with:
        path: _opam
        key: opam-${{ runner.os }}-ocaml-5.1.0-${{ hashFiles('.github/workflows/ci-concrete.yml') }}
        restore-keys: |
          opam-${{ runner.os }}-ocaml-5.1.0-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential

    - name: Install OCaml dependencies
      run: |
        opam install dune bignum 'menhir=20240715' 'menhirLib=20240715' core core_unix bisect_ppx yojson ppx_deriving_yojson

    - name: Run fast tests
      run: cd p4spec && opam exec -- dune build @speclang @p4parse @run-sl @sim-sl --profile=release

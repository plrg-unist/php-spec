open Lang

(* Stubs for lossy unboot conversions *)

let stub_dangle : Sl.dangle = false

(* instr note: instr.note / path.note carries the instruction id; lost in boot *)

let stub_instr_note : Sl.inote = { iid = 0 }

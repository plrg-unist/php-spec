;;
;;;; Static evaluation
;;;; of expressions that are local compile-time known
;;

relation Eval_static:
  cursor typingContext |- typedExpressionIR ~> value
  hint(input %0 %1 %2)
  hint(testgen_ignore)
  hint(prose_in "the local compile-time evaluation of" %2#", under cursor" %0 "and typing context" %1)

;;
;;;; Type evaluation
;;;; Inserts fresh type variables (`# tid*) for don't care type arguments (`_)
;;

relation Type_ok:
  cursor typingContext |- typeOrVoid : typeIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the type" %3 "and set of fresh type variables" %4)

relation TypeArgument_ok:
  cursor typingContext |- typeArgument : typeArgumentIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the type argument" %3 "and set of fresh type variables" %4)

relation TypeArguments_ok:
  cursor typingContext |- typeArgument* : typeArgumentListIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the type argument list" %3 "and set of fresh type variables" %4)

;;
;;;; Subtyping, explicit and implicit
;;

relation Sub_expl: typeIR <: typeIR
  hint(input %0 %1)
  hint(prose_true %0 "can be explicitly cast to" %1)
  hint(prose_false %0 "cannot be explicitly cast to" %1)
relation Sub_expl_canon: typeIR <: typeIR
  hint(input %0 %1)
  hint(prose_true %0 "can be explicitly cast to" %1 "after canonicalization")
  hint(prose_false %0 "cannot be explicitly cast to" %1 "after canonicalization")
relation Sub_expl_canon_neq: typeIR <: typeIR
  hint(input %0 %1)
  hint(prose_true %0 "can be explicitly cast to" %1 "when they are not the same type after canonicalization")
  hint(prose_false %0 "cannot be explicitly cast to" %1 "when they are not the same type after canonicalization")

relation Sub_impl: typeIR <: typeIR
  hint(input %0 %1)
  hint(prose_true %0 "can be implicitly cast to" %1)
  hint(prose_false %0 "cannot be implicitly cast to" %1)
relation Sub_impl_canon: typeIR <: typeIR
  hint(input %0 %1)
  hint(prose_true %0 "can be implicitly cast to" %1 "after canonicalization")
  hint(prose_false %0 "cannot be implicitly cast to" %1 "after canonicalization")
relation Sub_impl_canon_neq: typeIR <: typeIR
  hint(input %0 %1)
  hint(prose_true %0 "can be implicitly cast to" %1 "when they are not the same type after canonicalization")
  hint(prose_false %0 "cannot be implicitly cast to" %1 "when they are not the same type after canonicalization")

;;
;;;; Expression typing
;;

relation Expr_ok:
  cursor typingContext |- expression : typedExpressionIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the typed expression" %3)

;;
;;;; Argument typing
;;

relation Argument_ok:
  cursor typingContext |- argument : argumentIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the typed argument" %3)

;;
;;;; L-value typing
;;

relation Lvalue_ok:
  cursor typingContext |- lvalue : typedLvalueIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the typed lvalue" %3)

;;
;;;; Statement typing
;;

relation Stmt_ok:
  cursor typingContext flow |- statement : typingContext flow statementIR
  hint(input %0 %1 %2 %3)
  hint(prose_in "typing" %3#", under cursor" %0#", typing context" %1#", and abstract control flow" %2)
  hint(prose_out "\nthe typing context" %4#",\nthe abstract control flow" %5#",\nand the typed statement" %6)

relation BlockElementStmt_ok:
  typingContext flow |- blockElementStatement
                      : typingContext flow blockElementStatementIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and abstract control flow" %1)
  hint(prose_out "\nthe typing context" %3#",\nthe abstract control flow" %4#",\nand the typed block element statement" %5)

relation BlockElementStmts_ok:
  typingContext flow |- blockElementStatement*
                      : typingContext flow blockElementStatementIR*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and abstract control flow" %1)
  hint(prose_out "\nthe typing context" %3#",\nthe abstract control flow" %4#",\nand the typed block element statements" %5)

relation Block_ok:
  typingContext flow |- blockStatement
                      : typingContext flow blockStatementIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and abstract control flow" %1)
  hint(prose_out "\nthe typing context" %3#",\nthe abstract control flow" %4#",\nand the typed block statement" %5)

;;
;;;; Parameter typing
;;;; inserts fresh type variables for parameters with don't care types
;;

relation Parameter_ok:
  cursor typingContext |- parameter : parameterIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the parameter" %3 "and set of fresh type variables" %4)

relation Parameters_ok:
  cursor typingContext |- parameter* : parameterListIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the parameter list" %3 "and set of fresh type variables" %4)

relation ConstructorParameter_ok:
  cursor typingContext |- constructorParameter
                        : constructorParameterIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the constructor parameter" %3 "and set of fresh type variables" %4)

relation ConstructorParameters_ok:
  cursor typingContext |- constructorParameter*
                        : constructorParameterListIR `# tid*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the constructor parameter list" %3 "and set of fresh type variables" %4)

;;
;;;; Extern method/constructor typing
;;

relation ExternMethod_ok:
  typingContext tid |- externMethodPrototype
                     : typingContext externMethodPrototypeIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "for an extern object" %1)
  hint(prose_out "the updated typing context" %3 "and the typed method prototype" %4)

relation ExternMethods_ok:
  typingContext tid |- externMethodPrototype*
                     : typingContext externMethodPrototypeIR*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "for an extern object" %1)
  hint(prose_out "the updated typing context" %3 "and the typed method prototypes" %4)

relation ExternConstructor_ok:
  typingContext tid |- externConstructorPrototype
                     : typingContext externConstructorPrototypeIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "for an extern object" %1)
  hint(prose_out "the updated typing context" %3 "and the typed constructor prototype" %4)

relation ExternConstructors_ok:
  typingContext tid |- externConstructorPrototype*
                     : typingContext externConstructorPrototypeIR*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "for an extern object" %1)
  hint(prose_out "the updated typing context" %3 "and the typed constructor prototypes" %4)

;;
;;;; Parser typing
;;

relation ParserTransition_ok:
  typingContext nameIR* |- transitionStatement : transitionStatementIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and parser state names" %1)
  hint(prose_out "the typed transition statement" %3)

relation ParserStmt_ok:
  typingContext |- parserStatement : typingContext parserStatementIR
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed parser statement" %3)

relation ParserStmts_ok:
  typingContext |- parserStatement* : typingContext parserStatementIR*
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed parser statements" %3)

relation ParserState_ok:
  typingContext nameIR* |- parserState : parserStateIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and parser state names" %1)
  hint(prose_out "the typed parser state" %3)

relation ParserStates_ok:
  typingContext |- parserState* : parserStateIR*
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the typed parser states" %2)

relation ParserLocalDecl_ok:
  typingContext |- parserLocalDeclaration
                 : typingContext parserLocalDeclarationIR
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed parser local declaration" %3)

relation ParserLocalDecls_ok:
  typingContext |- parserLocalDeclaration*
                 : typingContext parserLocalDeclarationIR*
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed parser local declarations" %3)

;;
;;;; Control typing
;;

relation TableKey_ok:
  typingContext tableContext |- tableKey : tableContext tableKeyIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table key" %4)

relation TableKeys_ok:
  typingContext tableContext |- tableKey* : tableContext tableKeyListIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table keys" %4)

relation Call_action_partial_ok:
  typingContext |- parameterTypeIR* `@ argumentListIR
                 : parameterTypeIR* `, parameterTypeIR* `@ argumentListIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0)
  hint(prose_out "the typed argument list" %3)

relation TableAction_ok:
  typingContext tableContext |- tableAction : tableContext tableActionIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table action" %4)

relation TableActions_ok:
  typingContext tableContext |- tableAction* : tableContext tableActionListIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table actions" %4)

relation Call_action_default_ok:
  typingContext |- parameterTypeIR* `@ argumentListIR
                 : parameterTypeIR* `, parameterTypeIR* `@ argumentListIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0)
  hint(prose_out "the typed argument list" %3)

relation TableDefaultAction_ok:
  typingContext tableContext |- initializer : tableActionReferenceIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the typed default action" %3)

relation TableEntry_keyset_ok:
  typingContext tableContext |- keysetExpression : tableEntryState keysetExpressionIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed keyset expression" %4)

relation TableEntry_action_ok:
  typingContext tableContext |- tableActionReference : tableActionReferenceIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the typed table action reference" %3)

relation TableEntry_priority_ok:
  typingContext tableContext tableEntryState |- tableEntryPriority?
                                              : tableContext tableEntryPriorityOptIR
  hint(input %0 %1 %2 %3)
  hint(prose_in "typing" %3#", under typing context" %0 "and table context" %1
             #", and table entry state" %2)
  hint(prose_out "the updated table context" %4 "and the typed table entry priority" %5)

relation TableEntry_ok:
  typingContext tableContext |- tableEntry : tableContext tableEntryIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table entry" %4)

relation TableEntries_ok:
  typingContext tableContext |- tableEntry* : tableContext tableEntryListIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table entries" %4)

relation TableProperty_ok:
  typingContext tableContext |- tableProperty : tableContext tablePropertyIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table property" %4)

relation TableProperties_ok:
  typingContext tableContext |- tableProperty* : tableContext tablePropertyListIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated table context" %3 "and the typed table properties" %4)

relation Table_ok:
  typingContext |- tableProperty* : tableContext tablePropertyListIR
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed table" %3)

relation TableType_ok:
  typingContext tableContext |- name : typingContext typeIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under typing context" %0 "and table context" %1)
  hint(prose_out "the updated typing context" %3 "and the table type" %4)

relation ControlLocalDecl_ok:
  typingContext |- controlLocalDeclaration
                 : typingContext controlLocalDeclarationIR
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed control local declaration" %3)

relation ControlLocalDecls_ok:
  typingContext |- controlLocalDeclaration*
           : typingContext controlLocalDeclarationIR*
  hint(input %0 %1)
  hint(prose_in "typing" %1#", under typing context" %0)
  hint(prose_out "the updated typing context" %2 "and the typed control local declarations" %3)

;;
;;;; Declaration typing
;;

relation Decl_ok:
  cursor typingContext |- declaration : typingContext declarationIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1
             #",\nand the modified typing context")
  hint(prose_out "the modified typing context" %3 "and the typed declaration" %4)

relation Decls_ok:
  cursor typingContext |- declaration* : typingContext declarationIR*
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1
             #",\nand the modified typing context")
  hint(prose_out "the modified typing context" %3 "and the typed declarations" %4)

relation Program_ok:
  |- p4program : typingContext p4programIR
  hint(input %0)
  hint(prose_in "typing the P4 program" %0)
  hint(prose_out "the typing context" %1 "and the typed P4 program" %2)

;;
;;;; Call/instantiation convention
;;

;; Checks the copy-in/out calling convention,
;; and inserts implicit casts for arguments, if necessary
;; note that it depends on whether an action is being called or not

syntax actctxt = ACTION | NOACTION

relation Call_convention_expr_ok:
  cursor typingContext actctxt |- parameterTypeIR `@ typedExpressionIR
                                : typedExpressionIR
  hint(input %0 %1 %2 %3 %4)
  hint(prose_in "checking the calling convention of" %4 "applied to parameter type" %3
             #", under cursor" %0#", typing context" %1 #", and action context" %2)

relation Call_convention_argument_ok:
  cursor typingContext actctxt |- parameterTypeIR `@ argumentIR
                                : argumentIR
  hint(input %0 %1 %2 %3 %4)
  hint(prose_in "checking the calling convention of" %4 "applied to parameter type" %3
             #", under cursor" %0#", typing context" %1 #", and action context" %2)

relation Call_convention_ok:
  cursor typingContext actctxt |- parameterTypeIR* `@ argumentListIR
                                : argumentListIR
  hint(input %0 %1 %2 %3 %4)
  hint(prose_in "checking the calling convention of" %4 "applied to parameter types" %3
             #", under cursor" %0#", typing context" %1 #", and action context" %2)

;;
;;;; Routine typing
;;

;; Finds the type of a routine target,
;; inserts fresh type variables for parameters with don't care types, (tid*)
;; and also identifies the ids of default arguments (id*)

dec $is_static_routineTarget(routineTargetIR) : ctk

def $is_static_routineTarget(_ `. nameIR) = LCTK
  -- if nameIR <- [ "minSizeInBits", "minSizeInBytes",
                    "maxSizeInBits", "maxSizeInBytes" ]
def $is_static_routineTarget(TYPE _ `. nameIR) = LCTK
  -- if nameIR <- [ "minSizeInBits", "minSizeInBytes",
                    "maxSizeInBits", "maxSizeInBytes" ]
def $is_static_routineTarget(_) = DYN
  -- otherwise

relation RoutineTarget_ok:
  cursor typingContext |- routineTarget : routineTargetIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the typed routine target" %3)

relation RoutineTarget_lvalue_ok:
  cursor typingContext |- lvalue : routineTargetIR
  hint(input %0 %1 %2)
  hint(prose_in "typing" %2#", under cursor" %0 "and typing context" %1)
  hint(prose_out "the typed routine target" %3)

relation RoutineType_ok:
  cursor typingContext |- routineTargetIR `< typeArgumentListIR > `( argumentIR* )
                        : routineTypeIR `< `# tid* > `( `# id* )
  hint(input %0 %1 %2 %3 %4)

;; Checks that an action/function/method invocation is valid,
;; gets the return type,
;; infers the types of missing type arguments,
;; and inserts implicit casts for arguments, if necessary

relation Call_ok:
  cursor typingContext |- routineTypeIR `< typeArgumentListIR `# tid* >
                            `( argumentListIR `# id* )
                        : typeIR `< typeArgumentListIR > `( argumentListIR )
  hint(input %0 %1 %2 %3 %4 %5 %6)

;;
;;;; Constructor typing
;;

;; Finds the type of a constructor,
;; inserts fresh type variables for parameters with don't care types, (tid*)
;; and also identifies the ids of default arguments (id*)

relation ConstructorType_ok:
  cursor typingContext |- prefixedNameIR `< typeArgumentListIR > `( argumentIR* )
                        : constructorTypeIR `< `# tid* > `( `# id* )
  hint(input %0 %1 %2 %3 %4)

;; Checks that a constructor invocation is valid,
;; gets the return type,
;; infers the types of missing type arguments,
;; and inserts implicit casts for arguments, if necessary

syntax instctxt = NAMED | ANON

relation Inst_ok:
  cursor typingContext instctxt |- constructorTypeIR
                                    `< typeArgumentListIR `# tid* > `( argumentListIR `# id* )
                                 : typeIR
                                    `< typeArgumentListIR > `( argumentListIR )
  hint(input %0 %1 %2 %3 %4 %5 %6 %7)

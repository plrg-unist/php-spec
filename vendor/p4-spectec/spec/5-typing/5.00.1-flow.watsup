;;
;; Abstract Control Flow Tracker
;;

syntax flow = CONT | RET

var f : flow

dec $join_flow(flow, flow) : flow
  hint(prose_in "the merged control flow of" %0 "and" %1)
def $join_flow(RET, RET) = RET
def $join_flow(f_a, f_b) = CONT
  -- otherwise

; Workspace at repo root so tests can depend on spec and testdata.
(dirs docs p4spec spec excludes patches testdata p4c spec-meta towers)
(data_only_dirs docs spec excludes patches testdata p4c spec-meta towers)
